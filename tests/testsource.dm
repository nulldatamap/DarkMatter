
x : int = ( y + 3 ) * ( z - 3 );
const int PI = 788;
x = x + 0 - 0;
x = x * 0;
pi = PI;
tau = pi * 2 / 2 + ( 9 - 27 << 2 ); // 65464
pi = tua + 2;
n = 2 + 3 * ( pi = pi<<1 );
tua = p * n;
tua = ( x + 2 ) - 2
tau = tua * 8;
tua = tua / 128;

typedef #char str;

label gotohere;
goto gotohere;

loop
  x--;
while x < 10

struct user
  name : #char;
  age : int;
  id : uint // No need for semicolons
end

if x < 10 do
  x = 911;
else if x > 5
  y = x + z;
else
  print( "Dillermix!" );
end

func myFunc( a : int , b : int ) -> int
	return a + b;
end

